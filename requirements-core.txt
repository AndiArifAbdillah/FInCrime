# ============================================================
# FinCrime — Core requirements
# ============================================================
# This file contains only packages with broad Python-version
# wheel availability (works on Python 3.10 – 3.14).
#
# For full functionality (PyTorch autoencoder, GraphSAGE, SHAP,
# WeasyPrint PDF rendering) install requirements.txt instead —
# but those may need Python 3.11 or 3.12 on Windows.
# ============================================================

numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
scipy>=1.11.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Layer 0
xgboost>=2.0.0
joblib>=1.3.0

# Layer 2 fallback (networkx-only, no torch_geometric)
networkx>=3.2.0
neo4j>=5.15.0

# Layer 3 (templates only; PDF is optional)
jinja2>=3.1.2
python-dateutil>=2.8.2

# API
fastapi>=0.108.0
uvicorn[standard]>=0.25.0
httpx>=0.26.0
python-multipart>=0.0.6

# Dashboard
streamlit>=1.30.0
plotly>=5.18.0

# Observability
structlog>=24.1.0
prometheus-client>=0.19.0

# Testing
pytest>=7.4.0
faker>=22.0.0
